<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="08-fix-staff-limits-unique-constraint" author="claude-code">
        <comment>Drop unique constraint vì gây conflict khi deactivate</comment>
        
        <!-- Drop constraint cũ -->
        <dropUniqueConstraint 
            tableName="staff_limits" 
            constraintName="uk_staff_limits_name_active"/>

        <!-- Thêm comment giải thích tại sao không dùng constraint -->
        <!-- Logic unique sẽ được handle ở application level trong service -->
        
    </changeSet>

</databaseChangeLog>